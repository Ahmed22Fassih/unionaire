import 'package:flutter/material.dart';

class NotDoneScreen extends StatelessWidget {
  static const routeName = '/notDoneScreen';
  @override
  Widget build(BuildContext context) {
    return Scaffold(
        body: Center(
          child: Text(
              'not Done'
          ),
        ));
  }
}